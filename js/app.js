var express = require('express');
var app = angular.module("lm", []);

var id = [1, 2, 3, 4];

var grade = [9, 10, 11, 8];

var profile = [['Julia', 'Chrusciel'], ['Julian', 'Miller'], ['Ken', 'Barrios'], ['Matt', 'Duch']];
